{% extends "layout.html.njk" %}
{% block title %}Upraviť predmet{% endblock %}

{% block content %}
    <div class="container mt-4">

        <h2 class="mb-4 text-center">Upraviť predmet</h2>

        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card shadow-sm">
                    <div class="card-body">

                        <form method="POST"
                              action="/admin/subjects/edit/{{ subject.id_subject }}">

                            <div class="mb-3">
                                <label class="form-label">Názov predmetu</label>
                                <input type="text"
                                       name="name_subject"
                                       class="form-control"
                                       value="{{ subject.name_subject }}"
                                       required>
                            </div>

                            <div class="d-flex justify-content-between">
                                <a href="/admin/subjects" class="btn btn-secondary">
                                    Späť
                                </a>

                                <button type="submit" class="btn btn-primary">
                                    Uložiť zmeny
                                </button>
                            </div>

                        </form>

                    </div>
                </div>
            </div>
        </div>

    </div>
{% endblock %}
